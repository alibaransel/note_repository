import 'package:note_repository/constants/app_strings.dart';
import 'package:note_repository/models/message.dart';

class AppInfoMessages {
  const AppInfoMessages._();

  static const InfoMessage loginDone = InfoMessage(AppStrings.error);
  static const InfoMessage logoutDone = InfoMessage(AppStrings.error);
}
