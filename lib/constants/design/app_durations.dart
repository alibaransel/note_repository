class AppDurations {
  const AppDurations._();

  static const Duration s = Duration(milliseconds: 100);
  static const Duration m = Duration(milliseconds: 200);
  static const Duration l = Duration(milliseconds: 500);

  static const Duration splashMin = Duration(milliseconds: 500);
  static const Duration loadingIndicatorWait = Duration(milliseconds: 500);
  static const Duration snackBar = Duration(seconds: 2);
}
